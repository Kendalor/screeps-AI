import { Maintenance } from "./roles/Maintenance";

export const ROLE_STORE = {Maintenance};

export class CreepManager {
    public run(): void {
        for(const c in Game.creeps){
            // TODO
        }
    }
}